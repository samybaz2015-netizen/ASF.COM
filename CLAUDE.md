# مشروع عصف

مشروع مستقل تماماً عن برنامج **عطاء الجزيرة** (`E:\عطاء الجزيرة`).
لا تقرأ أو تعدّل أي ملف خارج `E:\Asf` إلا بطلب صريح من المستخدم.

## الوضع الحالي
- برنامج قائم بالفعل، سيتم **تحديثه** هنا (وليس بناؤه من الصفر).
- سير العمل: استيراد المصدر → تحديث → تشغيل محاكاة/اختبار محلي → رفع التحديث على استضافة **SmarterASP.NET**.

## هيكل المجلدات
- `src/`    — الكود المصدري للبرنامج القائم.
- `backup/` — نسخة من الإصدار الحالي قبل أي تعديل (لا تُعدَّل أبداً).
- `docs/`   — ملاحظات، بيانات الاستضافة والنشر، سجل التغييرات.

## الاستضافة
حساب SmarterASP.NET واحد (`asfconsult-002`) يضم ثلاثة مواقع — التفاصيل في `docs/hosting.md`:
- `site1` = api (عصف) · `site2` = frontend (عصف، asf-consulting.com) · `site3` = **ataa (عطاء)**

## قواعد مهمة
- **لا رفع إلى الاستضافة إلا بعد فحص وتقرير وموافقة صريحة من المستخدم** — قاعدة قطعية.
- **ممنوع** قراءة أو تعديل أو رفع أي شيء إلى `www/ataa` — هو برنامج عطاء على نفس الاستضافة.
- قبل أي تعديل: تأكد من وجود نسخة سليمة في `backup/`.
- لا تستخدم قاعدة بيانات عطاء (`AtaaDB`) ولا أي إعداد يخصّها.
- لا تُرفع أي كلمات مرور أو مفاتيح نشر داخل الكود؛ ضعها في ملفات `.env` المستثناة من Git.
